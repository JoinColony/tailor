/* @flow */

import type { RequiredContractDataProps } from './flowtypes';

// eslint-disable-next-line import/prefer-default-export
export const REQUIRED_CONTRACT_DATA_PROPS: RequiredContractDataProps = {
  abi: true,
  address: false,
  bytecode: false,
};
